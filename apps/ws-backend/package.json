{
  "name": "ws-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc -b",
    "start": "node ./dist/index.js",
    "dev": "pnpm --filter @repo/db run generate && npm run build && npm run start",
    "test": "vitest run --no-cache"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^22.10.7",
    "@types/ws": "^8.5.13",
    "vitest": "^2.1.8"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@repo/common": "workspace:*",
    "@repo/db": "workspace:*",
    "ws": "^8.18.0"
  }
}
